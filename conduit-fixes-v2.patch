diff --git a/frontend/apps/conduit/project.json b/frontend/apps/conduit/project.json
index 0d7efe5..854510b 100644
--- a/frontend/apps/conduit/project.json
+++ b/frontend/apps/conduit/project.json
@@ -48,6 +48,9 @@
     "serve": {
       "continuous": true,
       "executor": "@angular/build:dev-server",
+      "options": {
+        "proxyConfig": "proxy.conf.json"
+      },
       "configurations": {
         "production": {
           "buildTarget": "conduit:build:production"
diff --git a/frontend/apps/conduit/src/app/app.config.ts b/frontend/apps/conduit/src/app/app.config.ts
index 00fae0a..5480cad 100644
--- a/frontend/apps/conduit/src/app/app.config.ts
+++ b/frontend/apps/conduit/src/app/app.config.ts
@@ -3,6 +3,7 @@ import {
   provideBrowserGlobalErrorListeners,
 } from '@angular/core';
 import { provideRouter } from '@angular/router';
+import { provideHttpClient } from '@angular/common/http';
 import { appRoutes } from './app.routes';
 import { CONNECT_TRANSPORT, connectTransport } from '@conduit/shared-data-access';
 
@@ -10,6 +11,7 @@ export const appConfig: ApplicationConfig = {
   providers: [
     provideBrowserGlobalErrorListeners(),
     provideRouter(appRoutes),
+    provideHttpClient(),
     { provide: CONNECT_TRANSPORT, useValue: connectTransport },
   ],
 };
diff --git a/frontend/articles/data-access/src/lib/data-access/articles.service.ts b/frontend/articles/data-access/src/lib/data-access/articles.service.ts
index 47542b3..3ffd74d 100644
--- a/frontend/articles/data-access/src/lib/data-access/articles.service.ts
+++ b/frontend/articles/data-access/src/lib/data-access/articles.service.ts
@@ -1,17 +1,8 @@
 import { inject, Injectable } from "@angular/core";
-import { from, Observable } from "rxjs";
-import { createClient } from "@connectrpc/connect";
-import { ArticleService } from "@conduit/gen/articles";
-import { CONNECT_TRANSPORT } from "@conduit/shared-data-access";
-import type { 
-  ListArticlesResponse, 
-  GetArticleResponse, 
-  CreateArticleResponse, 
-  UpdateArticleResponse, 
-  DeleteArticleResponse,
-  FavoriteArticleResponse,
-  UnfavoriteArticleResponse
-} from "@conduit/gen/articles";
+import { HttpClient, HttpParams } from "@angular/common/http";
+import { Observable } from "rxjs";
+
+const API_BASE = "/conduit-api";
 
 export interface Article {
   slug: string;
@@ -26,44 +17,59 @@ export interface Article {
   author?: { username: string; bio: string; image: string; following: boolean };
 }
 
+interface ArticlesResponse {
+  articles: Article[];
+  articlesCount: number;
+}
+
+interface ArticleResponse {
+  article: Article;
+}
+
 @Injectable({ providedIn: "root" })
 export class ArticlesService {
-  private readonly transport = inject(CONNECT_TRANSPORT);
-  private readonly client = createClient(ArticleService, this.transport);
+  private http = inject(HttpClient);
+
+  getFeed(offset = 0, limit = 20): Observable<ArticlesResponse> {
+    const params = new HttpParams()
+      .set("limit", limit)
+      .set("offset", offset);
+    return this.http.get<ArticlesResponse>(`${API_BASE}/articles`, { params });
+  }
 
-  getFeed(offset = 0, limit = 20): Observable<ListArticlesResponse> {
-    return from(
-      this.client.listArticles({ offset, limit, tag: "", author: "", favorited: "" })
-    );
+  getByTag(tag: string, offset = 0, limit = 20): Observable<ArticlesResponse> {
+    const params = new HttpParams()
+      .set("tag", tag)
+      .set("limit", limit)
+      .set("offset", offset);
+    return this.http.get<ArticlesResponse>(`${API_BASE}/articles`, { params });
   }
 
-  getByTag(tag: string, offset = 0, limit = 20): Observable<ListArticlesResponse> {
-    return from(
-      this.client.listArticles({ offset, limit, tag, author: "", favorited: "" })
-    );
+  getArticle(slug: string): Observable<ArticleResponse> {
+    return this.http.get<ArticleResponse>(`${API_BASE}/articles/${slug}`);
   }
 
-  getArticle(slug: string): Observable<GetArticleResponse> {
-    return from(this.client.getArticle({ slug }));
+  createArticle(data: { title: string; description: string; body: string; tagList: string[] }): Observable<ArticleResponse> {
+    return this.http.post<ArticleResponse>(`${API_BASE}/articles`, { article: data });
   }
 
-  createArticle(data: { title: string; description: string; body: string; tagList: string[] }): Observable<CreateArticleResponse> {
-    return from(this.client.createArticle(data));
+  updateArticle(slug: string, data: { title?: string; description?: string; body?: string }): Observable<ArticleResponse> {
+    return this.http.put<ArticleResponse>(`${API_BASE}/articles/${slug}`, { article: data });
   }
 
-  updateArticle(slug: string, data: { title?: string; description?: string; body?: string }): Observable<UpdateArticleResponse> {
-    return from(this.client.updateArticle({ slug, ...data }));
+  deleteArticle(slug: string): Observable<void> {
+    return this.http.delete<void>(`${API_BASE}/articles/${slug}`);
   }
 
-  deleteArticle(slug: string): Observable<DeleteArticleResponse> {
-    return from(this.client.deleteArticle({ slug }));
+  favorite(slug: string): Observable<ArticleResponse> {
+    return this.http.post<ArticleResponse>(`${API_BASE}/articles/${slug}/favorite`, {});
   }
 
-  favorite(slug: string): Observable<FavoriteArticleResponse> {
-    return from(this.client.favoriteArticle({ slug }));
+  unfavorite(slug: string): Observable<ArticleResponse> {
+    return this.http.delete<ArticleResponse>(`${API_BASE}/articles/${slug}/favorite`);
   }
 
-  unfavorite(slug: string): Observable<UnfavoriteArticleResponse> {
-    return from(this.client.unfavoriteArticle({ slug }));
+  getTags(): Observable<{ tags: string[] }> {
+    return this.http.get<{ tags: string[] }>(`${API_BASE}/tags`);
   }
 }
diff --git a/frontend/articles/feature-article/src/lib/feature-article/feature-article.css b/frontend/articles/feature-article/src/lib/feature-article/feature-article.css
index cb55537..e802d59 100644
--- a/frontend/articles/feature-article/src/lib/feature-article/feature-article.css
+++ b/frontend/articles/feature-article/src/lib/feature-article/feature-article.css
@@ -1,3 +1,5 @@
+@reference "tailwindcss";
+
 .article-page {
   @apply min-h-screen bg-white;
 }
diff --git a/frontend/articles/feature-editor/src/lib/feature-editor/feature-editor.css b/frontend/articles/feature-editor/src/lib/feature-editor/feature-editor.css
index 41804ed..b99fc94 100644
--- a/frontend/articles/feature-editor/src/lib/feature-editor/feature-editor.css
+++ b/frontend/articles/feature-editor/src/lib/feature-editor/feature-editor.css
@@ -1,3 +1,5 @@
+@reference "tailwindcss";
+
 .editor-page {
   @apply min-h-screen bg-gray-50 py-12;
 }
diff --git a/frontend/articles/feature-feed/src/lib/feature-feed/feature-feed.css b/frontend/articles/feature-feed/src/lib/feature-feed/feature-feed.css
index 6564e74..7a55126 100644
--- a/frontend/articles/feature-feed/src/lib/feature-feed/feature-feed.css
+++ b/frontend/articles/feature-feed/src/lib/feature-feed/feature-feed.css
@@ -1,3 +1,5 @@
+@reference "tailwindcss";
+
 .feed-page {
   @apply min-h-screen bg-gray-50;
 }
diff --git a/frontend/articles/ui-article-preview/src/lib/ui-article-preview/ui-article-preview.css b/frontend/articles/ui-article-preview/src/lib/ui-article-preview/ui-article-preview.css
index e69de29..4df6392 100644
--- a/frontend/articles/ui-article-preview/src/lib/ui-article-preview/ui-article-preview.css
+++ b/frontend/articles/ui-article-preview/src/lib/ui-article-preview/ui-article-preview.css
@@ -0,0 +1,2 @@
+@reference "tailwindcss";
+
diff --git a/frontend/articles/ui-comments/src/lib/ui-comments/ui-comments.css b/frontend/articles/ui-comments/src/lib/ui-comments/ui-comments.css
index 828ff0c..a95056a 100644
--- a/frontend/articles/ui-comments/src/lib/ui-comments/ui-comments.css
+++ b/frontend/articles/ui-comments/src/lib/ui-comments/ui-comments.css
@@ -1,3 +1,5 @@
+@reference "tailwindcss";
+
 .comments-section {
   @apply mt-10;
 }
diff --git a/frontend/auth/feature-login/src/lib/feature-login/feature-login.css b/frontend/auth/feature-login/src/lib/feature-login/feature-login.css
index 8011560..0949e37 100644
--- a/frontend/auth/feature-login/src/lib/feature-login/feature-login.css
+++ b/frontend/auth/feature-login/src/lib/feature-login/feature-login.css
@@ -1,3 +1,5 @@
+@reference "tailwindcss";
+
 .auth-page {
   @apply py-12;
 }
diff --git a/frontend/auth/feature-login/src/lib/feature-login/feature-login.ts b/frontend/auth/feature-login/src/lib/feature-login/feature-login.ts
index c0dcf0a..0fa77f5 100644
--- a/frontend/auth/feature-login/src/lib/feature-login/feature-login.ts
+++ b/frontend/auth/feature-login/src/lib/feature-login/feature-login.ts
@@ -1,9 +1,10 @@
 import { Component, inject, signal, ChangeDetectionStrategy } from "@angular/core";
 import { Router, RouterLink } from "@angular/router";
+import { HttpClient } from "@angular/common/http";
 import { AuthStore } from "@conduit/auth-data-access";
-import { createClient } from "@connectrpc/connect";
-import { AuthService } from "@conduit/gen/auth";
-import { CONNECT_TRANSPORT } from "@conduit/shared-data-access";
+import { firstValueFrom } from "rxjs";
+
+const API_BASE = "/conduit-api";
 
 @Component({
   selector: "app-feature-login",
@@ -14,37 +15,40 @@ import { CONNECT_TRANSPORT } from "@conduit/shared-data-access";
 })
 export class FeatureLogin {
   private readonly authStore = inject(AuthStore);
-  private readonly router = inject(Router);
-  private readonly transport = inject(CONNECT_TRANSPORT);
-  private readonly client = createClient(AuthService, this.transport);
+  private readonly router    = inject(Router);
+  private readonly http      = inject(HttpClient);
 
-  protected readonly email = signal("");
+  protected readonly email    = signal("");
   protected readonly password = signal("");
-  protected readonly errors = signal<string[]>([]);
-  protected readonly loading = signal(false);
+  protected readonly errors   = signal<string[]>([]);
+  protected readonly loading  = signal(false);
 
   protected async onSubmit() {
     this.errors.set([]);
     this.loading.set(true);
 
     try {
-      const response = await this.client.login({
-        email: this.email(),
-        password: this.password(),
-      });
+      const response: any = await firstValueFrom(
+        this.http.post(`${API_BASE}/users/login`, {
+          user: { email: this.email(), password: this.password() },
+        })
+      );
 
-      if (response.user) {
-        this.authStore.setUser({
-          email: response.user.email,
-          token: response.user.token,
-          username: response.user.username,
-          bio: response.user.bio,
-          image: response.user.image,
-        });
+      if (response?.user) {
+        this.authStore.setUser(response.user);
         await this.router.navigate(["/"]);
       }
     } catch (error: any) {
-      this.errors.set([error.message || "Login failed"]);
+      const body = error?.error;
+      const msgs: string[] = [];
+      if (body?.errors) {
+        for (const [field, errs] of Object.entries(body.errors)) {
+          for (const msg of errs as string[]) {
+            msgs.push(`${field} ${msg}`);
+          }
+        }
+      }
+      this.errors.set(msgs.length ? msgs : ["Login failed. Please check your credentials."]);
     } finally {
       this.loading.set(false);
     }
diff --git a/frontend/auth/feature-register/src/lib/feature-register/feature-register.css b/frontend/auth/feature-register/src/lib/feature-register/feature-register.css
index 8011560..0949e37 100644
--- a/frontend/auth/feature-register/src/lib/feature-register/feature-register.css
+++ b/frontend/auth/feature-register/src/lib/feature-register/feature-register.css
@@ -1,3 +1,5 @@
+@reference "tailwindcss";
+
 .auth-page {
   @apply py-12;
 }
diff --git a/frontend/auth/feature-register/src/lib/feature-register/feature-register.ts b/frontend/auth/feature-register/src/lib/feature-register/feature-register.ts
index 04b1b2e..ec90ba2 100644
--- a/frontend/auth/feature-register/src/lib/feature-register/feature-register.ts
+++ b/frontend/auth/feature-register/src/lib/feature-register/feature-register.ts
@@ -1,9 +1,10 @@
 import { Component, inject, signal, ChangeDetectionStrategy } from "@angular/core";
 import { Router, RouterLink } from "@angular/router";
+import { HttpClient } from "@angular/common/http";
 import { AuthStore } from "@conduit/auth-data-access";
-import { createClient } from "@connectrpc/connect";
-import { AuthService } from "@conduit/gen/auth";
-import { CONNECT_TRANSPORT } from "@conduit/shared-data-access";
+import { firstValueFrom } from "rxjs";
+
+const API_BASE = "/conduit-api";
 
 @Component({
   selector: "app-feature-register",
@@ -14,39 +15,45 @@ import { CONNECT_TRANSPORT } from "@conduit/shared-data-access";
 })
 export class FeatureRegister {
   private readonly authStore = inject(AuthStore);
-  private readonly router = inject(Router);
-  private readonly transport = inject(CONNECT_TRANSPORT);
-  private readonly client = createClient(AuthService, this.transport);
+  private readonly router    = inject(Router);
+  private readonly http      = inject(HttpClient);
 
   protected readonly username = signal("");
-  protected readonly email = signal("");
+  protected readonly email    = signal("");
   protected readonly password = signal("");
-  protected readonly errors = signal<string[]>([]);
-  protected readonly loading = signal(false);
+  protected readonly errors   = signal<string[]>([]);
+  protected readonly loading  = signal(false);
 
   protected async onSubmit() {
     this.errors.set([]);
     this.loading.set(true);
 
     try {
-      const response = await this.client.register({
-        username: this.username(),
-        email: this.email(),
-        password: this.password(),
-      });
+      const response: any = await firstValueFrom(
+        this.http.post(`${API_BASE}/users`, {
+          user: {
+            username: this.username(),
+            email: this.email(),
+            password: this.password(),
+          },
+        })
+      );
 
-      if (response.user) {
-        this.authStore.setUser({
-          email: response.user.email,
-          token: response.user.token,
-          username: response.user.username,
-          bio: response.user.bio,
-          image: response.user.image,
-        });
+      if (response?.user) {
+        this.authStore.setUser(response.user);
         await this.router.navigate(["/"]);
       }
     } catch (error: any) {
-      this.errors.set([error.message || "Registration failed"]);
+      const body = error?.error;
+      const msgs: string[] = [];
+      if (body?.errors) {
+        for (const [field, errs] of Object.entries(body.errors)) {
+          for (const msg of errs as string[]) {
+            msgs.push(`${field} ${msg}`);
+          }
+        }
+      }
+      this.errors.set(msgs.length ? msgs : ["Registration failed. Please try again."]);
     } finally {
       this.loading.set(false);
     }
diff --git a/frontend/profile/data-access/src/lib/profile.service.ts b/frontend/profile/data-access/src/lib/profile.service.ts
index 0f250fa..3fe5e65 100644
--- a/frontend/profile/data-access/src/lib/profile.service.ts
+++ b/frontend/profile/data-access/src/lib/profile.service.ts
@@ -1,16 +1,8 @@
 import { inject, Injectable } from "@angular/core";
-import { from, Observable } from "rxjs";
-import { createClient } from "@connectrpc/connect";
-import { ProfileService as ConnectProfileService } from "@conduit/gen/profile";
-import { CONNECT_TRANSPORT } from "@conduit/shared-data-access";
-import type {
-  GetProfileRequest,
-  GetProfileResponse,
-  FollowRequest,
-  FollowResponse,
-  UnfollowRequest,
-  UnfollowResponse,
-} from "@conduit/gen/profile";
+import { HttpClient } from "@angular/common/http";
+import { Observable } from "rxjs";
+
+const API_BASE = "/conduit-api";
 
 export interface Profile {
   username: string;
@@ -19,20 +11,28 @@ export interface Profile {
   following: boolean;
 }
 
+interface ProfileResponse {
+  profile: Profile;
+}
+
 @Injectable({ providedIn: "root" })
 export class ProfileService {
-  private transport = inject(CONNECT_TRANSPORT);
-  private client = createClient(ConnectProfileService, this.transport);
+  private http = inject(HttpClient);
 
-  getProfile(username: string): Observable<GetProfileResponse> {
-    return from(this.client.getProfile({ username }));
+  getProfile(username: string): Observable<ProfileResponse> {
+    return this.http.get<ProfileResponse>(`${API_BASE}/profiles/${username}`);
   }
 
-  followUser(username: string): Observable<FollowResponse> {
-    return from(this.client.follow({ username }));
+  followUser(username: string): Observable<ProfileResponse> {
+    return this.http.post<ProfileResponse>(
+      `${API_BASE}/profiles/${username}/follow`,
+      {}
+    );
   }
 
-  unfollowUser(username: string): Observable<UnfollowResponse> {
-    return from(this.client.unfollow({ username }));
+  unfollowUser(username: string): Observable<ProfileResponse> {
+    return this.http.delete<ProfileResponse>(
+      `${API_BASE}/profiles/${username}/follow`
+    );
   }
 }
diff --git a/frontend/profile/feature-profile/src/lib/feature-profile/feature-profile.css b/frontend/profile/feature-profile/src/lib/feature-profile/feature-profile.css
index 66691d9..491651b 100644
--- a/frontend/profile/feature-profile/src/lib/feature-profile/feature-profile.css
+++ b/frontend/profile/feature-profile/src/lib/feature-profile/feature-profile.css
@@ -1,3 +1,5 @@
+@reference "tailwindcss";
+
 .profile-page {
   @apply min-h-screen bg-gray-50;
 }
diff --git a/frontend/profile/feature-profile/src/lib/profile-feature-profile/profile-feature-profile.css b/frontend/profile/feature-profile/src/lib/profile-feature-profile/profile-feature-profile.css
index e69de29..4df6392 100644
--- a/frontend/profile/feature-profile/src/lib/profile-feature-profile/profile-feature-profile.css
+++ b/frontend/profile/feature-profile/src/lib/profile-feature-profile/profile-feature-profile.css
@@ -0,0 +1,2 @@
+@reference "tailwindcss";
+
diff --git a/frontend/profile/feature-settings/src/lib/feature-settings/feature-settings.css b/frontend/profile/feature-settings/src/lib/feature-settings/feature-settings.css
index 8ea1695..1d412f5 100644
--- a/frontend/profile/feature-settings/src/lib/feature-settings/feature-settings.css
+++ b/frontend/profile/feature-settings/src/lib/feature-settings/feature-settings.css
@@ -1,3 +1,5 @@
+@reference "tailwindcss";
+
 .settings-page {
   @apply pt-8 pb-16;
 }
diff --git a/frontend/profile/feature-settings/src/lib/feature-settings/feature-settings.ts b/frontend/profile/feature-settings/src/lib/feature-settings/feature-settings.ts
index 75270cc..15fce48 100644
--- a/frontend/profile/feature-settings/src/lib/feature-settings/feature-settings.ts
+++ b/frontend/profile/feature-settings/src/lib/feature-settings/feature-settings.ts
@@ -1,13 +1,12 @@
 import { ChangeDetectionStrategy, Component, inject, OnInit, signal } from '@angular/core';
 import { CommonModule } from '@angular/common';
 import { FormsModule } from '@angular/forms';
-import { AuthStore } from '@conduit/auth-data-access';
-import { User, UpdateUserRequest } from '@conduit/gen/auth';
+import { HttpClient } from '@angular/common/http';
 import { Router } from '@angular/router';
-import { createClient } from "@connectrpc/connect";
-import { AuthService } from "@conduit/gen/auth";
-import { CONNECT_TRANSPORT } from "@conduit/shared-data-access";
-import { from, Observable } from "rxjs";
+import { AuthStore } from '@conduit/auth-data-access';
+import { firstValueFrom } from 'rxjs';
+
+const API_BASE = "/conduit-api";
 
 @Component({
   selector: 'conduit-feature-settings',
@@ -19,17 +18,18 @@ import { from, Observable } from "rxjs";
 })
 export class FeatureSettingsComponent implements OnInit {
   private authStore = inject(AuthStore);
-  private router = inject(Router);
-  private transport = inject(CONNECT_TRANSPORT);
-  private client = createClient(AuthService, this.transport);
+  private router    = inject(Router);
+  private http      = inject(HttpClient);
 
   user = this.authStore.user;
 
-  image = signal('');
+  image    = signal('');
   username = signal('');
-  bio = signal('');
-  email = signal('');
+  bio      = signal('');
+  email    = signal('');
   password = signal('');
+  errors   = signal<string[]>([]);
+  loading  = signal(false);
 
   ngOnInit() {
     const currentUser = this.user();
@@ -41,25 +41,40 @@ export class FeatureSettingsComponent implements OnInit {
     }
   }
 
-  updateSettings() {
-    const request = {
-      email: this.email(),
+  async updateSettings() {
+    this.errors.set([]);
+    this.loading.set(true);
+    const payload: any = {
+      email:    this.email(),
       username: this.username(),
-      bio: this.bio(),
-      image: this.image(),
-    } as UpdateUserRequest;
+      bio:      this.bio(),
+      image:    this.image(),
+    };
+    if (this.password()) payload.password = this.password();
 
-    if (this.password()) {
-      request.password = this.password();
+    try {
+      const token = this.authStore.token();
+      const response: any = await firstValueFrom(
+        this.http.put(`${API_BASE}/user`, { user: payload }, {
+          headers: token ? { Authorization: `Token ${token}` } : {},
+        })
+      );
+      if (response?.user) {
+        this.authStore.setUser(response.user);
+        this.router.navigate(['/profile', response.user.username]);
+      }
+    } catch (error: any) {
+      const body = error?.error;
+      const msgs: string[] = [];
+      if (body?.errors) {
+        for (const [field, errs] of Object.entries(body.errors)) {
+          for (const msg of errs as string[]) msgs.push(`${field} ${msg}`);
+        }
+      }
+      this.errors.set(msgs.length ? msgs : ['Update failed.']);
+    } finally {
+      this.loading.set(false);
     }
-
-    from(this.client.updateUser(request)).subscribe({
-      next: (res) => {
-        this.authStore.setUser(res.user);
-        this.router.navigate(['/profile', res.user?.username]);
-      },
-      error: (err) => console.error(err),
-    });
   }
 
   logout() {
@@ -67,4 +82,3 @@ export class FeatureSettingsComponent implements OnInit {
     this.router.navigate(['/']);
   }
 }
-
diff --git a/frontend/profile/ui-avatar/src/lib/profile-ui-avatar/profile-ui-avatar.css b/frontend/profile/ui-avatar/src/lib/profile-ui-avatar/profile-ui-avatar.css
index e69de29..4df6392 100644
--- a/frontend/profile/ui-avatar/src/lib/profile-ui-avatar/profile-ui-avatar.css
+++ b/frontend/profile/ui-avatar/src/lib/profile-ui-avatar/profile-ui-avatar.css
@@ -0,0 +1,2 @@
+@reference "tailwindcss";
+
diff --git a/frontend/shared/ui-layout/src/lib/footer.component.css b/frontend/shared/ui-layout/src/lib/footer.component.css
index 84ef862..b10a0d1 100644
--- a/frontend/shared/ui-layout/src/lib/footer.component.css
+++ b/frontend/shared/ui-layout/src/lib/footer.component.css
@@ -1,3 +1,5 @@
+@reference "tailwindcss";
+
 .footer {
   @apply bg-gray-100 mt-auto py-6;
 }
diff --git a/frontend/shared/ui-layout/src/lib/navbar.component.css b/frontend/shared/ui-layout/src/lib/navbar.component.css
index bea819b..3c075da 100644
--- a/frontend/shared/ui-layout/src/lib/navbar.component.css
+++ b/frontend/shared/ui-layout/src/lib/navbar.component.css
@@ -1,3 +1,5 @@
+@reference "tailwindcss";
+
 .navbar {
   @apply bg-white border-b border-gray-200;
 }
